otelcollector:
  enabled: true

apm_config:
  enabled: true
  apm_non_local_traffic: true

logs_enabled: true
log_level: debug

otlp_config:
  receiver:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

logs_config:
  force_use_http: true
