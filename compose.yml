services:
  otel-agent:
    build: .
    env_file: .env
    volumes:
      - ./datadog.yaml:/etc/datadog-agent/datadog.yaml
      - ./otel-config/:/otel-config
      - ./start.sh:/start.sh
    ports:
      - 4318:4318
